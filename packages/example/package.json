{
    "name": "object-oriented-c-example",
    "description": "A web example that runs OOC source through the language package",
    "private": true,
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "dev": "node esbuild.mjs --serve",
        "build": "tsc --noEmit && node esbuild.mjs",
        "preview": "node esbuild.mjs --serve"
    },
    "dependencies": {
        "object-oriented-c-language": "0.0.1",
        "langium": "~4.2.1"
    },
    "devDependencies": {
        "typescript": "~6.0.2",
        "esbuild": "~0.27.4"
    }
}
