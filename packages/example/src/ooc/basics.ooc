// 变量、基本类型与运算符
x = 42;
name = 'ooc';
flag = true;
empty = nil;
x + 3;

1 + 2;
7 - 3;
6 * 4;
7 % 3;
10 > 5;
10 >= 5;
10 == 10;
1 && 0;

// 字符串方法（空格调用）
'abcdef' slice 1 3;
'abc' + 'def';

// 字符串属性直接读取
'abcdef' length;

// JS 对象属性访问
Math PI;

// 注释：// 单行、/* 多行 */
