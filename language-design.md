# 面向对象编程语言设计文档

## 1. 核心语法和语义规则

### 1.1 基本理念

- **一切皆对象**：没有函数，只有对象和消息传递
- **消息传递**：通过向对象发送消息来执行操作
- **无类设计**：直接定义对象，没有类的概念
- **管道操作**：支持数据流的管道操作

### 1.2 消息传递语法

- **基本形式**：`对象 消息 参数1 参数2 ...`
- **示例**：
  - `'sdd' slice 1 2` (相当于 JavaScript 中的 `'sdd'.slice(1, 2)`)
  - `a f 1 2` (调用对象 a 的 f 消息，传入参数 1 和 2)
  - `a z` (调用对象 a 的 z 消息，无参数)

### 1.3 管道操作符

- **| 的链式操作 1**
  - 示例：`'abcdefg' slice 1 3 | startsWith 'b'` (相当于 JavaScript 中的 `'abcdefg'.slice(1, 3).startsWith('b')`).
  - 即类似传统的管道，在`|`前的表达式的计算结果，作为对象，传递给`|`后面的消息。
- **| 的链式操作 2 **
  - 示例：`2 | x . f 3 x` 等价于 `f 3 2`，即此处 x 是将前一步的计算结果，命名成 x，代入点后面的表达式。
- **|> 操作符**：将左侧对象作为右侧消息的第一个参数
  - 示例：`'abc' length |> f add 2` 等价于 js 中 `f.add(('abc'.length),2)`

### 1.4 字面量和基本数据类型

- **字符串**：`'hello'` (仅支持单引号字符串) 支持模板字符串，类似于`'hello,${name}'`
- **数字**：`42`、`3.14`
- **布尔值**：`true`、`false`
- **数组**：`[1, 2, 3]`
- **对象**：后面详细说明

### 1.5 注释

- **单行注释**：`// 这是一个注释`
- **多行注释**：`/* 这是一个多行注释 */`

### 1.6 表达式求值

- 所有表达式都返回一个对象
- 消息传递的结果是一个对象

## 2. 对象结构和消息传递机制

### 2.1 对象定义

- **对象字面量**：使用 `{}` 定义对象
- **消息处理器**：在对象内部定义消息处理器，格式为 `(消息名 参数1 参数2 ...){ ... }`

- **示例**：

```
a = {
    (f x y){
        x + y
    }
    (z){
        9
    }
}
```

对象的花括号里只允许消息定义这种结构，共享变量在闭包环境中维护。

### 2.3 作用域和闭包

- 对象可以捕获和保存其创建环境中的变量
- 消息处理器可以访问对象的内部状态

## 3. 变量绑定系统

### 3.1 常量绑定

- **语法**：`名称 = 表达式`
- **语义**：创建不可变绑定，类似于 JavaScript 中的 `const`
- **示例**：`a = 42`

### 3.2 可变绑定定义

- **语法**：`名称 := 表达式`
- **语义**：创建可变绑定，类似于 JavaScript 中的 `let`
- **示例**：`counter := 0`

### 3.3 可变绑定赋值

- **语法**：`名称 =: 表达式`
- **语义**：更新已有的可变绑定
- **示例**：`counter =: counter + 1`

### 3.4 绑定规则

- 绑定的作用域可以是全局或局部
- 局部绑定可以遮蔽外部同名绑定
